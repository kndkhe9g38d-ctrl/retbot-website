# RET Control Panel — GitHub Pages READY

Publish the contents of this folder as the GitHub Pages site for:
`https://kndkhe9g38d-ctrl.github.io/retbot-website`

The frontend contains no Discord bot token, client secret, admin token, MongoDB URI, or other private credential.

The API origin is configured to:
`https://retbot.wispbyte.app`

The OAuth login button sends users to the backend:
`https://retbot.wispbyte.app/auth/discord`

Do not change the backend API origin to an HTTP URL because the GitHub Pages site is HTTPS.
